import torch
import torch.nn as nn


class SelfAttention(nn.Module):
    def __init__(
            self,
            embed_size,
            heads):
        pass

    def forward(self, values, keys, queries):
        pass

